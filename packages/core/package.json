{
  "name": "@rapidkit/core",
  "version": "1.0.0",
  "private": true,
  "description": "Rapidkit — Monorepo backend core",
  "scripts": {
    "postinstall": "uv sync",
    "format": "uv run ruff check --fix src && uv run ruff format src",
    "typecheck": "uv run ty check src"
  }
}
