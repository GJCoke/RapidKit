# China mainland mirror sources for Docker build
# Usage: docker compose --env-file docker/.env.build.china build
REGISTRY_PREFIX=docker.1ms.run/
NPM_REGISTRY=https://registry.npmmirror.com
UV_IMAGE=ghcr.io/astral-sh/uv:latest
