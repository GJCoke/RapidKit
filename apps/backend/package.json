{
  "name": "@monorepo-example/backend",
  "version": "1.0.1",
  "scripts": {
    "lint:format": "uv run ruff check --fix src && uv run ruff format src",
    "lint:lint": "uv run mypy src",
    "typecheck": "uv run ty check src",
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 16000"
  },
  "engines": {
    "python": ">=3.14.0",
    "pip": ">=25.0.1"
  }
}
